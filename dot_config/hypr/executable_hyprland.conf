################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor = DP-1, 3840x2160@119.94, 0x0, 1, bitdepth, 10, vrr, 2, cm, hdr, sdrbrightness, 1.20
monitor = DP-2, 2560x1440@59.95, 3840x0, 1, bitdepth, 10, transform, 3


####################
### WORKSTATIONS ###
####################

workspace = 1, monitor:DP-1, persistent:true, default:true
workspace = 10, monitor:DP-2, persistent:true, default:true, layoutopt:orientation:top


###################
### MY PROGRAMS ###
###################

$terminal = ghostty
$fileManager = spf
$menu = walker
$game_menu = rofi -modi games -show games -theme games-smaller
$ms-edge = microsoft-edge-stable --password-store=kwallet -enable-features=UseOzonePlatform -ozone-platform=wayland
$signal = signal-desktop --use-tray-icon --ozone-platform-hint=auto --enable-features=WaylandWindowDecorations --enable-webrtc-pipewire-capturer
$youtube = flatpak run com.stremio.Stremio
$vpn = flatpak run com.protonvpn.www
$brave_args = --password-store=kwallet --enable-features=UseOzonePlatform,VaapiVideoDecoder,VaapiIgnoreDriverChecks,Vulkan,DefaultANGLEVulkan,VulkanFromANGLE,WebRTCPipeWireCapturer  -ozone-platform=wayland
$brave = brave $brave_args
$brave_incog = brave --incognito $brave_args
$calendar = $brave --app-id=ojibjkjikcpjonjjngfkegflhmffeemk
$cloud_docs = $brave --app-id=pmijndpkpdodmjcnaoaocgnlempcpplp
$cloud_drive = $brave --app-id=fnnddiokljlbkmeppnclajginnfbffgb
$mail = $brave --app-id=jnpecgipniidlgicjocehkhajgdnjekh
$pass = $brave --app-id=hkhckfoofhljcngmlnlojcbplgkcpcab
$slack = slack $brave_args


####################
###  OTHER VARS  ###
####################

$scripts = ~/.config/hypr/scripts
# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER


#################
### AUTOSTART ###
#################

# Auth and Keyring
exec-once = /usr/lib/polkit-kde-authentication-agent-1
exec-once = /usr/lib/pam_kwallet_init

# Clipboard
exec-once = wl-paste --type text --watch cliphist store #Stores only text data
exec-once = wl-paste --type image --watch cliphist store #Stores only image data

# Other
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = waybar & mako & hyprpaper & hyprctl dispatch workspace 1
exec-once = udiskie &
exec-once = walker --gapplication-service &


#############################
### ENVIRONMENT VARIABLES ###
#############################

# Toolkit Backend Variables
env = GDK_BACKEND, wayland
env = QT_QPA_PLATFORM, wayland;xcb
env = SDL_VIDEODRIVER, wayland
env = CLUTTER_BACKEND, wayland

# XDG Specifications
env = XDG_CURRENT_DESKTOP, Hyprland
env = XDG_SESSION_TYPE, wayland
env = XDG_SESSION_DESKTOP, Hyprland

# Qt Variables
env = QT_AUTO_SCREEN_SCALE_FACTOR, 1
env = QT_WAYLAND_DISABLE_WINDOWDECORATION, 1
env = QT_QPA_PLATFORMTHEME, qt6ct

env = SSH_ASKPASS, /usr/bin/ksshaskpass
env = SSH_ASKPASS_REQUIRE, prefer

# Other
env = ELECTRON_OZONE_PLATFORM_HINT, wayland
env = HYPRCURSOR_THEME, rose-pine-hyprcursor
env = HYPRCURSOR_SIZE, 24
env = XCURSOR_SIZE, 24
env = WLR_DRM_NO_ATOMIC, 1
env = WLR_NO_HARDWARE_CURSORS, 1

# Workaround for GTK4 bug
# env = GSK_RENDERER, ngl


#####################
### LOOK AND FEEL ###
#####################

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 10

    border_size = 2

    # col.active_border = rgb(7fb4ca) rgb(87a987) 45deg
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    layout = master

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 4
    rounding_power = 2

    active_opacity = 1.0
    inactive_opacity = 1.0

    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

cursor {
    inactive_timeout = 3
    hide_on_key_press = true
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = true # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    allow_small_split = true
    orientation = center
    slave_count_for_center_master = 0
    center_master_fallback = left
    mfact = 0.45
}

misc {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    disable_hyprland_logo = true
    disable_splash_rendering = true
    force_default_wallpaper = 0
    #focus_on_activate = true
}



#############
### INPUT ###
#############

input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1
    #mouse_refocus = false
    
    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    # workspace_swipe = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, T, exec, $terminal
bind = $mainMod, Q, killactive
bind = $mainMod, F, exec, $terminal -e $fileManager
bind = $mainMod SHIFT, F, exec, nautilus
bind = $mainMod, V, togglefloating, 
bind = $mainMod, R, exec, $menu
bind = $mainMod SHIFT, R, exec, $game_menu
#bind = $mainMod, P, pseudo, # dwindle
#bind = $mainMod, J, togglesplit, # dwindle


bind = $mainMod, A, exec, pwvucontrol

# Start your web browser
bind = $mainMod, B, exec, $brave
bind = $mainMod ALT, B, exec, $brave_incog

bind = $mainMod SHIFT, B, exec, google-chrome-stable --password-store=kwallet5 --ozone-platform-hint=auto --ozone-platform=wayland
#bind = $mainMod SHIFT, B, exec, chromium --password-store=kwallet5 --ozone-platform-hint=auto --ozone-platform=wayland
# bind = $mainMod CTRL, B, exec, firefox
# bind = $mainMod ALT, B, exec, $ms-edge --inprivate

# Passwords
# bind = $mainMod, P, exec, $brave --app-id=hkhckfoofhljcngmlnlojcbplgkcpcab
bind = $mainMod, P, exec, $pass
bind = $mainMod SHIFT, P, exec, kwalletmanager5
bind = $mainMod CTRL, P, exec, zsh -c secret

# Games
bind = $mainMod, G, exec, lutris
bind = $mainMod SHIFT, G, exec, steam
bind = $mainMod ALT, G, exec, ludusavi

# Media
bind = $mainMod, Y, exec, freetube
#bind = $mainMod, Y, exec, $ms-edge --app-id=agimnkijcaahngcdmfeangaknmldooml
bind = $mainMod SHIFT, Y, exec, stremio

# VPN
bind = $mainMod SHIFT, V, exec, $proton_vpn

# Programming
bind = $mainMod SHIFT, C, exec, code --ozone-platform-hint=auto --enable-features=WaylandWindowDecorations
# bind = $mainMod, C, exec, cursor

# Data
bind = $mainMod, D, exec, $cloud_drive
bind = $mainMod SHIFT, D, exec, $cloud_docs

# Messaging
bind = $mainMod, M, exec, $signal
bind = $mainMod SHIFT, M, exec, $slack
# bind = $mainMod ALT, M, exec, teams-for-linux --enable-features=WebRTCPipeWireCapturer
bind = $mainMod CTRL, M, exec, discord --ozone-platform-hint=auto --enable-features=WebRTCPipeWireCapturer

# BLUETOOTH
# bind = $mainMod, F5, exec, zsh -c bt-stunners-connect

# Email
# bind = $mainMod, E, exec, $brave --app-id=jnpecgipniidlgicjocehkhajgdnjekh
bind = $mainMod, E, exec, $mail
# Calendar
bind = $mainMod, C, exec, $calendar

# Note taking
bind = $mainMod, N, exec, logseq --ozone-platform=wayland --enable-features=UseOzonePlatform,WaylandWindowDecorations

# System
bind = $mainMod, F10, exec, systemctl suspend
bind = $mainMod SHIFT, F10, exec, shutdown now -r 
bind = $mainMod CTRL, F10, exec, shutdown now

# Screen Capturing
bind = $mainMod, X, exec, zsh -c $scripts/screen-capture-area-copy.zsh
bind = $mainMod CTRL, X, exec, zsh -c $scripts/screen-capture-area-note.zsh
bind = $mainMod SHIFT, X, exec, zsh -c $scripts/screen-record-area.zsh
# bind = $mainMod, X, exec, zsh -c screen-capture-window-copy

# Monitor
bind = $mainMod SHIFT, F1, exec, $scripts/monitor-1-default.zsh
bind = $mainMod ALT, F1, exec, $scripts/monitor-1-capture.zsh
# bind = $mainMod, F1, exec, zsh -c $scripts/monitor-side-enable.zsh
# bind = $mainMod SHIFT, F1, exec, zsh -c $scripts/monitor-side-disable.zsh
# bind = $mainMod, F2, exec, zsh -c $scripts/speakers-tv.zsh
# bind = $mainMod SHIFT, F2, exec, zsh -c $scripts/speakers-stunners.zsh
# bind = $mainMod, F3, exec, zsh -c $scripts/monitor-bitdepth-10.zsh
# bind = $mainMod SHIFT, F3, exec, zsh -c $scripts/monitor-bitdepth-8.zsh
# bind = $mainMod, F5, exec, zsh -c $scripts/monitor-refresh-120.zsh
# bind = $mainMod SHIFT, F5, exec, zsh -c $scripts/monitor-refresh-60.zsh

# Audio
# bind = $mainMod, F4, exec, zsh -c $scripts/speakers-tv.zsh
bind = $mainMod ALT, Y, exec, tidal-hifi
bind = $mainMod SHIFT, F2, exec, zsh -c $scripts/restar-pipewire-audio.zsh

# Other Tools
bind = $mainMod, A, exec, $ms-edge --app-id=eoijjdmppnkkcdhjpnnalmhdfjhkiidi

# Media Controls
bind = $mainMod, F8, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bind = $mainMod, F7, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bind = $mainMod, F6, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bind = $mainMod, F9, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bind = $mainMod, F2, exec, brightnessctl -e4 -n2 set 5%+
bind = $mainMod, F1, exec, brightnessctl -e4 -n2 set 5%-

bind = $mainMod, F4, exec, playerctl next
bind = $mainMod, F5, exec, playerctl play-pause
bind = $mainMod, F3, exec, playerctl previous

# Master Layout Controls
bind = $mainMod, Z, layoutmsg, focusmaster
bind = $mainMod SHIFT, Z, layoutmsg, swapwithmaster
bind = $mainMod ALT, Z, layoutmsg, addmaster
bind = $mainMod CTRL, Z, layoutmsg, removemaster

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

bind = $mainMod SHIFT, H, movewindow, l
bind = $mainMod SHIFT, L, movewindow, r
bind = $mainMod SHIFT, K, movewindow, u
bind = $mainMod SHIFT, J, movewindow, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow


# Window Rules

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:

# windowrule = immediate, title:^(THE FINALS)$
# windowrule = immediate, title:^(Pal)$
# #windowrule = workspace 2, class:^(Slack)$
# #windowrule = workspace 2, class:^(signal)$
# windowrule = workspace 2, class:^(obsidian)$
# windowrule = workspace 1, title:^(Path of Exile)$
# windowrule = immediate, title:^(Path of Exile)$
# windowrule = immediate, title:^(DOOMEternal)$
# windowrule = immediate, title:^(Need for Speed™ Heat)$
# windowrule = immediate, title:^(Baldur's Gate 3)$
# windowrule = immediate, title:^(Cyberpunk 2077)$
# windowrule = immediate, title:^(Resident Evil 4)$
